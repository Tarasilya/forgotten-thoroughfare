// #include "render_queue.h"


// namespace component {

// void RenderQueue::Push(Drawable* drawable, int layer) {
//     queue_.push({-layer, drawable});
// }

// Drawable* RenderQueue::Pop() {
//     if (queue_.empty()) {
//         return 0;
//     }
//     auto result = queue_.top().second;
//     queue_.pop();
//     return result;
// }

// }
